This app is a simple Spring based test app without andy database access.

The app has a feature where you can force the app to crash on startup to test app crashes.
Set an env variable test=true to enable this and the app will fail with an out-of-memory error.
